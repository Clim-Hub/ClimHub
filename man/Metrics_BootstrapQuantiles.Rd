% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Metrics_BootstrapQuantiles.r
\name{Metrics_BootstrapQuantiles}
\alias{Metrics_BootstrapQuantiles}
\title{Bootstrap Quantile Lookup for Daily Time Series}
\usage{
Metrics_BootstrapQuantiles(
  CFVariable,
  probs = c(0.1, 0.9),
  bootstrapWindow = 5
)
}
\arguments{
\item{CFVariable}{\code{CFVariable} object for which quantiles for each bootstrap day interval ought to be calculated.}

\item{probs}{Numeric vector of probabilities (between 0 and 1) for which quantiles will be returned.  Default is \code{c(0.1,0.9)}.}

\item{bootstrapWindow}{Odd integer giving the width of the window in days used for the bootstrap sample around each target date. The window is centred on the day of year of interest.  Defaults to 5.}
}
\value{
A numeric array with dimensions \code{lon × lat × dayOfYear × probs}.  The third dimension has 366 levels ("01‑01" through "12‑31", including Feb 29) and the fourth dimension is named by the supplied \code{probs}.  Values are the requested quantiles computed over the set of years that fall inside the bootstrap window.  If no valid observations exist for a given cell/day the result is NA.
}
\description{
Computes quantiles across years for each day‑of‑year using a rolling bootstrap window to be used in \code{Metrics_ETTCDI} calculation of ETCCDI. Internally the function uses \code{Helper_ETCCDIDailyBootstraps()} to determine which dates belong in the window centred on each calendar day (assuming a leap year for consistency).

This is primarily used by the ETCCDI metric code to obtain base period thresholds but can be called directly by users. Dates in the input \code{CFVariable} must present individual days.
}
\examples{
\dontrun{
TX <- NC_Read("inst/extdata/KiN_tasmax.nc")[["tasmax"]]
TX_Quant <- Metrics_BootstrapQuantiles(CFVariable = TX, probs = c(0.1, 0.9), bootstrapWindow = 5)
TX_Quant
}
}
\author{
Erik Kusch
}
